package com.timbuchalka;

public class Main {

    public static void main(String[] args){

        /*Scanner scanner = new Scanner(System.in);//read user input

        System.out.println("Enter yob:");

        boolean hasNextInt = scanner.hasNextInt();

        if (hasNextInt){
            int yearOfBirth = scanner.nextInt();
            scanner.nextLine(); //nadle next line character

            System.out.println("Enter your name: ");
            String name = scanner.nextLine();//RELEASES UNDERLYING MEMOMRY SCANNER WAS USING EARLIER;
            int age = 2020 - yearOfBirth;

            if(age >= 0 && age <= 100){
                System.out.println("your name is " + name + ", and you are " + age + " years old");
            }else
                System.out.println("Errorrrrrrr");
        }else {
            System.out.println("Unable to parse year of birth");
        }

        scanner.close(); //*/


    }
}
